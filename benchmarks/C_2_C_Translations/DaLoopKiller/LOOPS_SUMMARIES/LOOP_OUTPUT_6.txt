>> Summary loop for: ./examples/input.c:11
==========================================

Basic Block ( while.cond )

    tmp1 = s.addr
    tmp2 = [ tmp1 ]
    tobool = ( tmp2 != 0 )
    if ( tobool != 0) { goto while.body }
    if ( tobool == 0) { goto while.end }

Basic Block ( while.body )

    tmp3 = s.addr
    incdec.ptr = tmp3 + 1
    s.addr = incdec.ptr
    goto while.cond
